node_modules/
firebase-debug.log

# Copied into place by firebase.json's predeploy hook right before every
# functions deploy — the real source of truth is the root mission-engine.js;
# this is a generated duplicate, never hand-edited, never committed.
functions/mission-engine.js
functions/nextdns-config.js
